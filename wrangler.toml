name = "my-blog"
pages_build_output_dir = "dist"
build.command = "npm install"

[[d1_databases]]
binding = "DB"
database_name = "blog-db"
database_id = "60469428-d10b-4b86-a819-cb7e0c7e3e25"

# 敏感配置请通过以下方式设置，不要写在本文件中：
# 本地开发：创建 .dev.vars 文件（已加入 .gitignore）
# 生产环境：运行 wrangler pages secret put JWT_SECRET / ADMIN_PASSWORD
